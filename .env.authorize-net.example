# Authorize.Net Configuration
# Add these environment variables to your .env file

# Authorize.Net Environment (sandbox or production)
AUTHORIZE_NET_ENVIRONMENT=sandbox

# For production, use: production
# AUTHORIZE_NET_ENVIRONMENT=production

# Note: API Login ID and Transaction Key are stored per-company in the database
# They are configured through the Settings → Payments UI

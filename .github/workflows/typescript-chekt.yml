name: TypeScript Check

on:
  pull_request:
    branches: [master, development]
  push:
    branches: [master, development]

jobs:
  typescript-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          # Change from "18" to "20" (or "22" for the current LTS)
          node-version: "20"
          cache: "yarn"

      - name: Install Dependencies
        # 2. Combined redundant steps and used --frozen-lockfile for CI integrity
        # Use 'yarn install --immutable' if you are using Yarn Berry (v2+)
        run: yarn install --frozen-lockfile

      - name: TypeScript check
        # 3. Using yarn to execute the local tsc binary
        run: yarn tsc --noEmit
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"
